# ============================================================
# Sentinel-Ops Local Environment Configuration
# ============================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.local.template .env.local
#
# REQUIRED: Only Odoo credentials are mandatory
# OPTIONAL: BigQuery/Slack are completely optional for local dev
# ============================================================

# --- REQUIRED: Odoo Connection (Production) ---
# Your Odoo instance URL (must include https://)
ODOO_URL=https://odoo.alohas.com
ODOO_DB=odoo.alohas.io
ODOO_USERNAME=your-email@alohas.com
ODOO_PASSWORD=your-api-key-or-password

# --- RECOMMENDED: Development Odoo (for testing create_documents) ---
# Used by create_documents job with use_dev=true (default)
# Same credentials work, just different URL/database
ODOO_DEV_URL=https://developalohas17.odoogest.com
ODOO_DEV_DB=developalohas17.odoogest.com
# ODOO_DEV_USERNAME=  # Optional: falls back to ODOO_USERNAME
# ODOO_DEV_PASSWORD=  # Optional: falls back to ODOO_PASSWORD

# --- OPTIONAL: BigQuery Audit Trail ---
# Leave commented for local development (uses console logging instead)
# Uncomment and set for full audit trail in BigQuery
#GCP_PROJECT=alohas-data-project
#BQ_DATASET=sentinel_ops

# --- OPTIONAL: Slack Alerts ---
# Leave commented to disable Slack notifications
#SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
#SLACK_CHANNEL=#alohas-automation-announcements

# --- API Security ---
# API key for authenticating HTTP requests (n8n, MCP, external callers)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SENTINEL_API_KEY=your-api-key-here

# --- FTP Connection (for packing list PDFs) ---
FTP_HOST=ftp.rmtrade.es
FTP_USERNAME=alohas
FTP_PASSWORD=your-ftp-password
FTP_PORT=21
FTP_USE_TLS=false

# --- Runtime Settings ---
ENVIRONMENT=development
LOG_LEVEL=INFO
# Set to DEBUG for verbose output: LOG_LEVEL=DEBUG
